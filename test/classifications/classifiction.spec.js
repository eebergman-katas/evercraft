import Classification from '../../src/js/classifications/classification';
const expect = require('chai').expect;

describe('Classification Spec', () => {
    it('should fail', () => {
        expect(true).to.be.false;
    })
});
