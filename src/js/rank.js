"use strict";

const defaultXP = 0,
    defaultLevel = 1;

export default class Rank {

    constructor(xp, level) {
        this.xp = xp       || defaultXP;
        this.level = level || defaultLevel;
    }



}